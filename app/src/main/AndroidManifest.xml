<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.sunday">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TestBarcodeActivity"></activity>
        <activity android:name=".HomeIntroActivity" />
        <activity android:name=".BarcodeGeneratorActivity" />
        <activity
            android:name=".NavHomeActivity"
            android:label="@string/title_activity_nav_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".RemoverNewActivity" />
        <activity android:name=".EditProductsActivity" />
        <activity android:name=".ProductsScannedActivity" />
        <activity android:name=".ScanProductsBarActivity" />
        <activity android:name=".ScanBarcodeActivity" />
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:screenOrientation="fullSensor"
            android:stateNotNeeded="true"
            tools:replace="android:screenOrientation" />
    </application>

</manifest>